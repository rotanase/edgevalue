import 'package:flutter/material.dart';
import 'package:edgevalue/widgets/logo.dart' as widgets;

class NavigationBarLogo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return widgets.Logo(
      height: 80,
      width: 150,
    );
  }
}
